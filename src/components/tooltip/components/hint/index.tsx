import { FC } from "react";

import { TooltipHintProps } from "../..";

/**
 * Tooltip.Hint — slot that declares the popup content.
 * Rendered by the parent Tooltip; returns null when used standalone.
 */
const TooltipHint: FC<TooltipHintProps> = () => null;

export default TooltipHint;
