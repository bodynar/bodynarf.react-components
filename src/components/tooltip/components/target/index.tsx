import { FC } from "react";

import { TooltipTargetProps } from "../..";

/**
 * Tooltip.Target — slot that declares the anchor element.
 * Rendered by the parent Tooltip; returns null when used standalone.
 */
const TooltipTarget: FC<TooltipTargetProps> = () => null;

export default TooltipTarget;
