import { FC } from "react";

/** Loader placeholder displayed while page content is loading */
const PageLoader: FC = () => (
    <div style={{ padding: "2rem", textAlign: "center" }}>
        Loading examples...
    </div>
);

export default PageLoader;
