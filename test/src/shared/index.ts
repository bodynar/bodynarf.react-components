import { ElementColor, ElementSize } from "@bodynarf/react.components";

/** All possible sizes as string, separated by comma */
export const AllSizesString = Object.keys(ElementSize).join(", ");

/** All possible element colors as string, separated by comma */
export const AllColorsString = Object.keys(ElementColor).join(", ");